services:
  - type: web
    name: bombaylane-demo
    env: node
    buildCommand: npm install
    startCommand: node demo-server.js
    envVars:
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
